@api
Feature: Deleting Events
  As the user
  I should be able to delete events

# Test that authenticated users can not delete events.
# Test that administrator users can delete events.
# Test that group managers can delete events from their group.
# Test that members can ONLY delete their own events.