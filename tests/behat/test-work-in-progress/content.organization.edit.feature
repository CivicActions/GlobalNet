@api
Feature: Editing Events
  As the user
  I should be able to edit events

#### Access
# Test that authenticated users can not edit events.
# Test that admins can edit events.
# Test that group members can not edit events.
# Test that group members can edit their own events.
# Test that group managers can edit events.
# Test that group managers can only edit events from their own group. 

#### Field configuration
# Test that required fields are required
# Test that interactions with the form work as desired (hiding/showing fields)
# - Test that only administrator can see the og_group_ref field.

#### Field permissions
# Test that group managers can only see the Private, Group, and Organization
# options for the simple access field.